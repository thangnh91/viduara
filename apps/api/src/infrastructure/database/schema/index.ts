// Schema barrel — import all tables here as they are created
// Example: export * from "./users";
// Example: export * from "./sessions";

// Placeholder — full schema added in subsequent tasks (Day 2+)
export {};
